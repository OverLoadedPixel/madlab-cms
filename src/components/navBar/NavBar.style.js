import styled from "styled-components";
import { Menu } from "antd";


export const NavBarStyle = styled(Menu)`
  display: flex;
  justify-content: flex-end;
  width: auto;
  padding: 0 10px;
  position: relative;
  right: 0;
   align-items: center;
  
  
`;
